# Agent 1 — Spec & Contract Authority (Laravel 12 / MySQL)
Goal: Convert Phase‑0 YAMLs into authoritative contracts:
- specs/<context>/openapi.json (OpenAPI 3.1)
- specs/<context>/schemas/*.json (JSON Schemas)
- specs/<context>/rbac.matrix.json (role × action)
- specs/<context>/events.yml (domain events)

Place your Phase‑0 folder at `shipping_suite_documentation/**` in the repo root.
